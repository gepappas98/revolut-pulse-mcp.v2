{
  "mcpServers": {
    "mcprice-local": {
      "_comment": "Local mode — run directly with uv (no Docker needed)",
      "command": "uv",
      "args": [
        "run",
        "--with", "fastmcp,httpx,yfinance",
        "python", "app.py"
      ],
      "cwd": "/REPLACE/WITH/YOUR/PATH/revolut-pulse-mcp.v2"
    },
    "mcprice-remote": {
      "_comment": "Remote mode — point to your deployed Fly.io / Railway URL",
      "type": "streamable-http",
      "url": "https://mcprice.fly.dev/mcp"
    }
  }
}
